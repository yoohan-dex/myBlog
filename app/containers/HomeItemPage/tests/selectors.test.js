import {
  selectHomeItemPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectHomeItemPage();

describe('selectHomeItemPage', () => {

});
